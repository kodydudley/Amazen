export default class Product {
  constructor(title, description, picture, price, id, creatorId) {
    this.title = title
    this.description = description
    this.picture = picture
    this.price = price
    this.id = id
    this.creatorId = creatorId
  }
}
